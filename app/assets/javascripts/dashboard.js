/*var contextDashboard = addClass.create( {
    initialize : function(container)
    {
      this.form = container;
      this.filter_project = this.container.down('#filter_project');
      this.filter_project.observe('change', this.changeFilterAndSubmit.bindAsEventListener(this));
    },
    changeFilterAndSubmit : function () {
      this.submit();
    }
});
*/